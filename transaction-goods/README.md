## 商品中心

```
2019-4-19：添加缓存,对于商品业务做出处理

首先扣除redis缓存库存数量, 库存不足直接拒绝发送消息.
也就是说,发送消息,进行业务处理的操作已将扣除redis库存,并且数据库库存足够此订单,
所以当失败时(账户,订单,库存操作),需要将失败的库存数量重新添加到redis

代码:redisApi.increase(RedisConfig.GOODS_COUNT + id, count);
```

